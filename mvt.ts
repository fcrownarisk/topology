const m1 = [1][0][0][0];
const m2 = [0][1][0][0];
const m3 = [0][0][1][0];
const m4 = [0][0][0][1];
let v1 = new (m1[0].toString)
let v2 = new (m2[0].toString)
let v3 = new (m3[0].toString)
let v4 = new (m4[0].toString)
let arr = [m1,m2,m3,m4]
let array  = [v1,v2,v3,v4]
let arraylist = arr*array;
setInterval (function(){
 setInterval.toString()
    void function (momentum){
        'm1*v1'+'m2*v2'<='m3*v3'+'m4*v4'
      return momentum;
     }
    void function (energy){
        (1%2);'m1*v1*v1'+(1%2);'m2*v2*v2'<=(1%2);'m3*v3*v3'+(1%2);'m4*v4*v4'
      return energy;
 }
    void function(equation){
        "E = m * c * c"
      return equation
    }
})
setTimeout (function(){
    setTimeout.toLocaleString()
    void 'loop';{
        for(let i = 0;i< arr.length;i++)
            for(let j = 0; j < array.length;j++)
                for(let k = 0; k < arraylist; k++)
       }
})
